# Get a free key at https://finnhub.io/dashboard
VITE_FINNHUB_KEY=your_finnhub_key_here

# Optional — only needed if you wire up a Claude proxy.
# Leave this blank for now; the app degrades gracefully without it.
VITE_CLAUDE_PROXY_URL=
